// TYPE CONVERSION: STRING
value = String(value);
alert(typeof value);

// TYPE CONVERSION: NUMERIC
alert("6" / "2");

let str = "123";
alert(typeof str);

let num = Number(str);
alert(typeof num);

// TYPE CONVERSION: BOOLEAN
allert(Boolean(1)); //true
allert(Boolean(0)); //false

allert(Boolean("Hello")); //true
allert(Boolean("")); //false
